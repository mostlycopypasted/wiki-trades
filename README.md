# Trading Wiki

Trading and market research

This is an LLM-managed wiki. The LLM (Claude) owns `wiki/`. I curate sources in `raw/`. The schema is in `AGENTS.md`.

## Layout

- `raw/` — source documents I've collected
- `wiki/` — LLM-generated pages
- `AGENTS.md` — schema for the LLM
